/**
 * Placeholder savings copy — compute from billing data when membership + usage is live in Supabase.
 */
export function estimatedYouthBlockSavingsUsd(): number {
  return 0
}

export function estimatedFieldRentalSavingsUsd(): number {
  return 0
}
