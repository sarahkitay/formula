/**
 * Bumped when admin chrome / nav changes. Present on live HTML as `data-formula-admin-shell`
 * on the admin AppShell root (curl or DevTools should show this on production after deploy).
 */
export const FORMULA_ADMIN_SHELL_MARKER = 'nav-select-v2'
