import { ParentBookAssessmentLayoutClient } from '@/components/parent/parent-book-assessment-layout-client'

export default function ParentBookAssessmentLayout({ children }: { children: React.ReactNode }) {
  return <ParentBookAssessmentLayoutClient>{children}</ParentBookAssessmentLayoutClient>
}
